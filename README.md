# SAR Simulation

This project simulates a search-and-rescue drone scanning for wireless emitters.

## Quickstart

```bash
python cli/run_sim.py --config configs/example.yaml
python cli/run_post.py --config configs/example.yaml
```

Outputs are written to `runs/`.

## Configuration

See `configs/example.yaml` for the available parameters.
